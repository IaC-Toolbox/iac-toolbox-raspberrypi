# Generated by iac-toolbox — do not edit by hand
name: Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build_docker_image:
    runs-on: ubuntu-latest
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and Push Container Image
        run: |
          TAG=${{ github.sha }}
          docker build -f ./Dockerfile \
            -t vvasylkovskyi1/iac-toolbox-cli:$TAG \
            -t vvasylkovskyi1/iac-toolbox-cli:latest \
            .
          docker push vvasylkovskyi1/iac-toolbox-cli:$TAG
          docker push vvasylkovskyi1/iac-toolbox-cli:latest
