---
- name: Restart cadvisor
  shell: source {{ iac_shell_profile }} 2>/dev/null; docker compose restart cadvisor
  args:
    executable: "{{ iac_shell_executable }}"
    chdir: "{{ cadvisor_base_dir }}"
