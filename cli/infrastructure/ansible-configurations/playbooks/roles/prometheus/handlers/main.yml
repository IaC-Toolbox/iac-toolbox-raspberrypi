---
- name: Reload systemd
  systemd:
    daemon_reload: yes
  when: ansible_service_mgr == "systemd"

- name: Restart prometheus
  systemd:
    name: prometheus
    state: restarted
  when: ansible_service_mgr == "systemd"
