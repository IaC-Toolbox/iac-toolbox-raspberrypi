---
- name: Reload systemd
  systemd:
    daemon_reload: yes
  when: ansible_service_mgr == "systemd"

- name: Restart loki
  systemd:
    name: loki
    state: restarted
  when: ansible_service_mgr == "systemd"
