# Generated by iac-toolbox
# Safe to commit — no secrets stored here
device:
  profile: app-server
docker:
  enabled: true
github_build_workflow:
  enabled: true
  docker_hub_username: vvasylkovskyi1
  docker_image_name: vvasylkovskyi1/iac-toolbox-cli
  docker_hub_token: "{{ docker_hub_token }}"
github_runner:
  enabled: false
cloudflare:
  enabled: true
  mode: api
  account_id: 6010b62692a2aa521314ec448f67fb92
  zone_id: f595ac9a556083288bc7fbe8a6dc2598
  tunnel_name: mac-test-tunnel
  cloudflare_api_token: "{{ cloudflare_api_token }}"
  domains:
    - hostname: mac.iac-toolbox.com
      service_port: 9999
      service: http://localhost:9999
vault:
  enabled: false
grafana:
  enabled: false
prometheus:
  enabled: true
  version: latest
  base_dir: ~/.iac-toolbox/observability
  port: 9090
  scrape_interval: 15s
  retention: 15d
  grafana_url: https://grafana.iac-toolbox.com
loki:
  enabled: false
pagerduty:
  enabled: false
target:
  host: raspberry-4b.local
  user: vvasylkovskyi
  ssh_key: ~/.ssh/raspberrypi-4b
node_exporter:
  enabled: true
grafana_alloy:
  enabled: true
  alloy_remote_write_url: https://grafana.iac-toolbox.com/prometheus/api/v1/write
