# Generated by iac-toolbox
# Safe to commit — no secrets stored here
device:
  profile: platform
docker:
  enabled: true
cloudflare:
  enabled: true
  mode: api
  account_id: 6010b62692a2aa521314ec448f67fb92
  zone_id: f595ac9a556083288bc7fbe8a6dc2598
  tunnel_name: main-backend-tunnel
  cloudflare_api_token: "{{ cloudflare_api_token }}"
  domains:
    - hostname: grafana.iac-toolbox.com
      service_port: 3000
      service: http://localhost:3000
    - hostname: prometheus.iac-toolbox.com
      service_port: 9090
      service: http://localhost:9090
    - hostname: loki.iac-toolbox.com
      service_port: 3100
      service: http://localhost:3100
vault:
  enabled: false
target:
  mode: local
grafana:
  enabled: true
  version: latest
  base_dir: "{{ ansible_env.HOME }}/.iac-toolbox/grafana"
  port: 3000
  domain: grafana.iac-toolbox.com
  url: https://grafana.iac-toolbox.com
  provisioning_dir: "{{ ansible_env.HOME }}/.iac-toolbox/grafana/provisioning"
  prometheus_port: 9090
  prometheus_domain: prometheus.iac-toolbox.com
  admin_user: admin
  admin_password: "{{ grafana_admin_password }}"
  memory_limit: 512m
prometheus:
  enabled: true
  version: latest
  base_dir: "{{ ansible_env.HOME }}/.iac-toolbox/prometheus"
  domain: prometheus.iac-toolbox.com
  port: 9090
  scrape_interval: 15s
  retention: 15d
  grafana_url: https://grafana.iac-toolbox.com
  memory_limit: 256m
loki:
  enabled: true
  port: 3100
  retention_hours: 168
  base_dir: "{{ ansible_env.HOME }}/.iac-toolbox/loki"
  domain: loki.iac-toolbox.com
node_exporter:
  enabled: true
cadvisor:
  enabled: true
  memory_limit: 128m
grafana_alloy:
  enabled: true
  alloy_remote_write_url: https://prometheus.iac-toolbox.com/api/v1/write
  memory_limit: 256m
node_metrics_alerts:
  enabled: true
  terraform_dest: /home/vvasylkovskyi/.iac-toolbox/infrastructure/terraform/node-alerts
container_metrics_alerts:
  enabled: true
  service_name: grafana
  terraform_dest: /home/vvasylkovskyi/.iac-toolbox/infrastructure/terraform/container-alerts
grafana_pagerduty:
  enabled: true
  service_region: eu
  service_name: grafana
  terraform_dest: /home/vvasylkovskyi/.iac-toolbox/infrastructure
  pagerduty_token: "{{ pagerduty_token }}"
