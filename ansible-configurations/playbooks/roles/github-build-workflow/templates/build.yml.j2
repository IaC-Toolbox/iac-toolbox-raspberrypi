# Generated by iac-toolbox — do not edit by hand
name: Build

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  build_docker_image:
    runs-on: [self-hosted, ARM64]
    steps:
      - name: Check out the repo
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: {% raw %}${{ secrets.DOCKER_USERNAME }}{% endraw %}
          password: {% raw %}${{ secrets.DOCKER_PASSWORD }}{% endraw %}

      - name: Build and Push Container Image
        run: |
          TAG={% raw %}${{ github.sha }}{% endraw %}
          docker build -f ./Dockerfile \
            -t {{ github_build_workflow.docker_image_name }}:$TAG \
            -t {{ github_build_workflow.docker_image_name }}:latest \
            .
          docker push {{ github_build_workflow.docker_image_name }}:$TAG
          docker push {{ github_build_workflow.docker_image_name }}:latest
