---
- name: Restart vault
  systemd:
    name: vault
    state: restarted
    daemon_reload: yes
  when: ansible_service_mgr == "systemd"

- name: Reload systemd
  systemd:
    daemon_reload: yes
  when: ansible_service_mgr == "systemd"
