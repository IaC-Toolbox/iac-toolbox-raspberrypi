---
# ============================================
# IaC Toolbox - cAdvisor Playbook
# ============================================
# Deploys cAdvisor for Docker container metrics.

- name: Deploy cAdvisor
  hosts: all
  become: false
  roles:
    - role: cadvisor
      when: cadvisor is not defined or cadvisor.enabled | default(true)
