---
# ============================================
# IaC Toolbox - PostgreSQL Playbook
# ============================================
# Deploys PostgreSQL in a Docker container with persistent data volume.

- import_playbook: common.yml

- name: Deploy PostgreSQL
  hosts: all
  become: false
  roles:
    - role: postgresql
      when: postgresql is not defined or postgresql.enabled | default(true)
